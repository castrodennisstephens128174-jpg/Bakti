items:
  # Native asset (kept from the upstream template — used by the SEP-24 demo UI)
  - id: stellar:native
    distribution_account: GDXLIJKLBCE55IYHSBKAWSTPU5CP4NBP4XGRMYHR4CJE4UEJQTTPVCQG
    significant_decimals: 7
    sep6:
      enabled: true
      deposit:
        enabled: true
        min_amount: 0
        max_amount: 10
        methods:
          - SEPA
          - SWIFT
      withdraw:
        enabled: true
        min_amount: 0
        max_amount: 10
        methods:
          - bank_account
          - cash
    sep24:
      enabled: true
      deposit:
        enabled: true
        min_amount: 0
        max_amount: 10
        methods:
          - SEPA
          - SWIFT
      withdraw:
        enabled: true
        min_amount: 0
        max_amount: 10
        methods:
          - bank_account
          - cash
    sep31:
      enabled: false
    sep38:
      enabled: true
      exchangeable_assets:
        - iso4217:USD

  # USDC — the asset Bakti's SEP-31 client sends. SEP-31 receive is enabled here,
  # standing in for PeraHub's real PHP cash-out until PeraHub's own testnet
  # endpoint is available (see anchor-platform/README.md).
  - id: stellar:USDC:GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5
    distribution_account: GDXLIJKLBCE55IYHSBKAWSTPU5CP4NBP4XGRMYHR4CJE4UEJQTTPVCQG
    significant_decimals: 2
    sep31:
      enabled: true
      quotes_supported: false
      quotes_required: false
      receive:
        min_amount: 0
        max_amount: 10000
        methods:
          - CASH

  # Fiat (kept from the upstream template, unused by Bakti's SEP-31 flow)
  - id: iso4217:USD
    significant_decimals: 4
    sep31:
      enabled: false
      receive:
        min_amount: 0
        max_amount: 1000000
        methods:
          - SEPA
          - SWIFT
    sep38:
      enabled: true
      exchangeable_assets:
        - stellar:native
      country_codes:
        - US
      sell_delivery_methods:
        - name: WIRE
          description: Send USD directly to the Anchor's bank account.
      buy_delivery_methods:
        - name: WIRE
          description: Have USD sent directly to your bank account.
